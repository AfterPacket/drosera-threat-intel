# ==============================================================
# Drosera Honeypot — Firewalla MSP Block List
# Author : AfterPacket
# TLP    : TLP:WHITE
# Source : Drosera honeypot / static analysis
# ==============================================================
#
# Paste into Firewalla MSP → Rules → Target List.
# Annotated for human review. The unannotated machine-readable
# feed is ../blocklist.txt.
#
# Confidence tags:
#   HIGH   — direct honeypot capture (delivery IP) or config-block C2
#   MEDIUM — hardcoded array C2, not observed live
#   LOW    — packed/obfuscated sample, extraction uncertain
#
# No shared CDN ranges (Cloudflare, AWS, Fastly, GitHub, Akamai)
# are ever added here — they break legitimate traffic.
#
# ⚠ PORT RULES ARE NOT IN THIS FILE.
# A Target List cannot express a port. The IRC C2 channel used by
# PERLBOT_SHELLBOT (tcp/6667) is NOT blocked by anything below, and
# that bot accepts a C2 override at runtime — so the IP entries here
# do not contain it. See drosera_port_policy.txt in this directory
# for the rules to create manually. Rule 1 is the important one.
#
# ── Entries begin below; one section per family ───────────────


# ══════════════════════════════════════════════════════════════
# CAPTURE 2026-08-07 — drosera-loot.zip, 25 samples
# ══════════════════════════════════════════════════════════════

# ── MIRAI_OHSHIT — telnet dropper + payload host (HIGH — direct capture) ──
# Serves /ohshit.sh and //bot.<arch> across 15 architectures.
94.154.43.123

# ── MIRAI_TELNETCURL — payload server (HIGH — hardcoded in curl.sh/wget.sh) ──
# Serves /arm /arm5 /arm7 /mips /mipsel
205.237.110.232

# ── MIRAI_TELNETCURL — telnet dropper delivery (HIGH — direct capture) ──
60.185.49.73
138.117.43.19

# ── MIRAI_LOADER — loader host (HIGH — SERVER= in config block) ──
77.90.185.66

# ── PERLBOT_SHELLBOT — IRC C2 (HIGH — config block, port 6667) ──
213.139.77.150

# ── PERLBOT_SHELLBOT — ssh dropper delivery (HIGH — direct capture) ──
160.30.204.101
77.90.185.42
54.37.11.139

# ── GSOCKET_SSHIT — exfil endpoint (HIGH — POST /gsocket/up.php in config) ──
192.253.248.9

# ── GSOCKET_SSHIT — ssh dropper delivery (HIGH — direct capture) ──
192.253.248.92

# ── Mirai loader droppers, misc telnet (HIGH — direct capture) ──
195.123.171.185
123.145.11.38
27.137.233.190
211.219.254.187
175.198.110.15

# ── WEBROOT_PROBE — /var/www/html/filter campaign (HIGH — 224 sightings) ──
# Write-and-execute capability probes over ssh, 2026-07-28 → 2026-08-05.
# Rotates ~2 IPs/day inside five bulletproof /24s. If your policy permits
# CIDR blocks, the /24s below are a better fit than individual hosts:
#   2.57.122.0/24  92.118.39.0/24  80.94.92.0/24
#   195.178.110.0/24  193.32.162.0/24
2.57.122.209
2.57.122.168
92.118.39.77
92.118.39.50
92.118.39.71
92.118.39.49
92.118.39.14
80.94.92.234
80.94.92.55
80.94.92.179
195.178.110.217
195.178.110.232
195.178.110.228
195.178.110.227
193.32.162.84
193.32.162.34
193.32.162.15
193.32.162.27

# ══════════════════════════════════════════════════════════════
# ELF PAYLOAD EXTRACTION — added after string extraction
# ══════════════════════════════════════════════════════════════

# ── MIRAI_OHSHIT — C2 domains (HIGH — hardcoded in all 7 ELF payloads) ──
# Every one is named to impersonate legitimate infrastructure. Identical
# set across all seven architecture builds, so this is compiled-in config.
api-relay-3.metrics-collector.io
cdn-edge-updates.hostcloud-eu.net
mgmt-panel.serverstats-daemon.com
sync.softwaremirror.workers.dev
glibc.malloc.top
#
# Also present, NOT listed above — cannot be blocked at DNS/IP layer:
#   control.tor2web-relay-fast.onion
# .onion names do not resolve through normal DNS. Block Tor egress and
# tor2web gateways instead if this matters in your estate.
#
# CONSIDER, not included here: the apex domains metrics-collector.io,
# hostcloud-eu.net, serverstats-daemon.com and malloc.top are very likely
# wholly attacker-owned, and blocking the apex would survive subdomain
# rotation. They are omitted because that is not verified and this is a
# public feed. Verify registration before blocking an apex.
# Do NOT block the apex workers.dev — that is Cloudflare Workers, shared
# infrastructure. Only the specific subdomain above is attacker-controlled.

# ── MIRAI_OHSHIT — C2 IPs (HIGH — hardcoded in all 7 ELF payloads) ──
45.61.161.207
45.83.140.28
5.101.221.87
51.15.68.114
94.130.53.201
195.201.24.6

# ── PERLBOT_SHELLBOT — second IRC C2 (HIGH — config block) ──
# Used by /dodu and /gots. Distinct from /duba's 213.139.77.150.
213.177.179.11

# ── DO NOT BLOCK ──────────────────────────────────────────────
# These appear in sample source but are legitimate shared
# infrastructure. The gsocket sample abuses a real security tool;
# attribute the abuse, not the tool.
#   gsocket.io   thc.org   github.com   ifconfig.me
#
# Extracted from the MIRAI_OHSHIT ELF payloads and deliberately
# EXCLUDED from the block lists — these are shared CDN and would
# break large amounts of legitimate traffic:
#   185.199.108.153   GitHub Pages
#   104.21.234.17     Cloudflare
#   workers.dev       Cloudflare Workers apex (the specific
#                     sync.softwaremirror subdomain IS blocked)
#   bugs.launchpad.net  glibc build boilerplate, not infrastructure
#
# Not real hosts — placeholders and network addresses picked up by
# the extractor, excluded on inspection:
#   1.2.3.4      documentation placeholder
#   131.0.0.0    network address, not a host
#   119.0.0.0  120.0.0.0  121.0.0.0   Mirai scanner range constants
#
# Mirai family fingerprint, NOT live infrastructure — present in all
# five MIRAI_TELNETCURL payloads. It is the decoy domain from the
# original leaked Mirai source. Useful for attribution; blocking it
# achieves nothing:
#   www.ikindalikemenbutonlyontuesday.com
#
# ══════════════════════════════════════════════════════════════
# ⚠⚠ CRITICAL — DO NOT BLOCK — added 2026-08-08
# ══════════════════════════════════════════════════════════════
# The MIRAI_OHSHIT ELF payloads carry an XOR-0x22 obfuscated HTTP flood
# module. Deobfuscating it yields twenty domain names. THEY ARE ALL
# LEGITIMATE SITES, used as randomised Referer header values by the
# flood — they are not infrastructure and must never reach this file:
#
#   www.google.com     www.facebook.com   twitter.com
#   www.instagram.com  www.reddit.com     www.youtube.com
#   www.bing.com       www.linkedin.com   t.co
#   duckduckgo.com     search.yahoo.com   www.tiktok.com
#   www.pinterest.com  www.baidu.com      www.cloudflare.com
#   github.com         stackoverflow.com  news.ycombinator.com
#   discord.com        telegram.org       www.useragentstring.com
#
# Any pipeline that extracts domains from malware and appends them to a
# blocklist WILL ingest these. Blocking them takes the estate off the
# internet. This is the single most damaging error available in this
# dataset — more damaging than missing a C2, because it is self-inflicted
# and immediate.
#
# ══════════════════════════════════════════════════════════════
# SINKHOLE NOTE
# ══════════════════════════════════════════════════════════════
# Blocking is containment; sinkholing is intelligence. The five
# MIRAI_OHSHIT C2 domains above are compiled identically into every
# architecture build, so DNS-level control of the four attacker-owned
# apexes (metrics-collector.io, hostcloud-eu.net, serverstats-daemon.com,
# malloc.top) covers all 15 architectures — including the 8 never
# captured. Do NOT attempt this on workers.dev: that apex is Cloudflare
# shared infrastructure, and the correct route is a Cloudflare abuse
# report against sync.softwaremirror.workers.dev alone.
#
# The GSOCKET exfil endpoint 192.253.248.9 is the richest sinkhole
# target here: every beacon carries victim hostname, public IP and the
# gs-netcat access credential. See
# ../reports/CAPTURE_20260807_infrastructure_and_sinkhole.md
